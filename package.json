{
  "name": "cache-router-skill",
  "version": "0.1.0",
  "description": "Provider-aware cache router CLI",
  "type": "module",
  "bin": {
    "cache-router": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
