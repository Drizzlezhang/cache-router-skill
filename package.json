{
  "name": "@drizzlezhang/cache-router-skill",
  "version": "0.1.1",
  "description": "Provider-aware cache router CLI",
  "type": "module",
  "bin": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.mjs",
    "start": "node dist/index.js",
    "prepublishOnly": "npm test && npm run build"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
